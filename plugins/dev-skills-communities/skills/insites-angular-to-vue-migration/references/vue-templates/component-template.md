# Vue component template (use for migrated `.vue` files)

Copy this structure when generating a migrated component. File name: **kebab-case** (e.g. `my-feature-page.vue`). **Never** use `<style scoped>`.

See [`Component.vue`](Component.vue) for the full template (kept in sync with this doc).
