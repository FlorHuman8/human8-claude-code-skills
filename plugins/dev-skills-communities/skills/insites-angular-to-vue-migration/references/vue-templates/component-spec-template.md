# Jest spec template (use for migrated `__tests__/*.spec.ts`)

Path: `__tests__/my-feature-page.spec.ts` with `import MyFeaturePage from '../my-feature-page.vue'`.

Use **Jest** and **Composition API** mocks only (no `vi.*`, no `mocks: { $router }`).

See [`Component.spec.ts`](Component.spec.ts) for the full template (kept in sync with this doc).

Also see [`Component.spec.ts`](Component.spec.ts) in this folder (keep in sync with this doc).
